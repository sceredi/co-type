apiVersion: v1
kind: Service
metadata:
  name: gameserver
  namespace: cotype
spec:
  clusterIP: None
  selector:
    app: gameserver
  ports:
    - port: {{ .Values.matchPort }}
      targetPort: {{ .Values.matchPort }}
